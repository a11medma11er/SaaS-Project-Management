# تعليمات تشغيل Seeders

## 📊 ملخص البيانات الوهمية

تم تحديث Seeders لإنشاء بيانات وهمية أكبر وأكثر واقعية:

### 👥 المستخدمون (Users)
- **العدد الإجمالي:** 20 مستخدم
  - 1 Super Admin
  - 1 Admin
  - 1 Manager
  - 1 User
  - 16 مستخدم إضافي (متنوعين بين Manager و User)

**الأقسام:** Development, Design, Marketing, QA, DevOps, Support
**المناصب:** Senior, Junior, Lead, Staff

**بيانات تسجيل الدخول:**
- جميع المستخدمين: `password`
- Email format: `[position].[department][number]@example.com`
- مثال: `senior.development1@example.com`

### 📁 المشاريع (Projects)
- **العدد الإجمالي:** 52 مشروع
  - 20 مشروع قيد التنفيذ (Inprogress) - 40%
  - 16 مشروع مكتمل (Completed) - 30%
  - 16 مشاريع متوقفة/لم تبدأ (On Hold) - 30%

**ملاحظة:** الحالات المتاحة في قاعدة البيانات: `Inprogress`, `Completed`, `On Hold`

### 📋 المهام (Tasks)
- **العدد لكل مشروع:** 8-15 مهمة
- **العدد الإجمالي التقريبي:** ~572 مهمة (52 مشروع × متوسط 11 مهمة)
- كل مهمة يمكن أن يكون لها:
  - 1-3 مستخدمين معينين
  - 1-5 tags
  - 2-6 subtasks (60% من المهام)
  - 1-3 time entries (للمهام النشطة)

### 🤖 قرارات الذكاء الاصطناعي (AI Decisions)
- **التوزيع:**
  - 50% من المهام لديها 1-2 قرار AI
  - 40% من المشاريع لديها 1-2 قرار AI
  - 20 قرار عام (غير مرتبط بمشروع أو مهمة)
- **العدد الإجمالي التقريبي:** ~250-300 قرار AI

### 💬 التعليقات (Comments)
- تعليقات المشاريع: على 8 مشاريع عشوائية (2-5 تعليقات لكل مشروع)
- تعليقات المهام: على ~140 مهمة عشوائية (1-4 تعليقات لكل مهمة)
- 30% من التعليقات لديها ردود

## 🚀 طريقة التشغيل

### الطريقة الأولى: إعادة ضبط قاعدة البيانات بالكامل
```bash
php artisan migrate:fresh --seed
```
هذا الأمر سيقوم بـ:
1. حذف جميع الجداول
2. إعادة إنشاء الجداول
3. تشغيل جميع Seeders تلقائياً

### الطريقة الثانية: تشغيل Seeders بشكل منفصل
```bash
# 1. الأدوار والصلاحيات (يجب تشغيله أولاً)
php artisan db:seed --class=RolesAndPermissionsSeeder

# 2. صلاحيات الذكاء الاصطناعي
php artisan db:seed --class=AIPermissionsSeeder

# 3. المستخدمون الافتراضيون (20 مستخدم)
php artisan db:seed --class=DefaultUserSeeder

# 4. البيانات الوهمية (المشاريع، المهام، AI، التعليقات)
php artisan db:seed --class=DemoDataSeeder
```

### الطريقة الثالثة: تشغيل Seeder واحد فقط
```bash
# إعادة تشغيل البيانات الوهمية فقط (بعد حذفها يدوياً)
php artisan db:seed --class=DemoDataSeeder
```

## ⚠️ ملاحظات هامة

1. **الترتيب مهم:** يجب تشغيل `RolesAndPermissionsSeeder` قبل `DefaultUserSeeder`

2. **وقت التنفيذ:** قد يستغرق `DemoDataSeeder` 2-3 دقائق بسبب كمية البيانات الكبيرة

3. **الذاكرة:** تأكد من أن `memory_limit` في PHP كافي (على الأقل 512M)

4. **البيئة:** يُفضل استخدام هذا في بيئة التطوير فقط (Development Environment)

5. **قاعدة البيانات:** تأكد من إعداد قاعدة البيانات بشكل صحيح في `.env`:
   ```
   DB_CONNECTION=mysql
   DB_HOST=127.0.0.1
   DB_PORT=3306
   DB_DATABASE=your_database_name
   DB_USERNAME=your_username
   DB_PASSWORD=your_password
   ```

## 📈 إحصائيات متوقعة بعد التشغيل

| العنصر | العدد التقريبي |
|--------|----------------|
| Users | 20 |
| Projects | 52 |
| Tasks | ~572 |
| AI Decisions | ~250-300 |
| Project Comments | ~35 |
| Task Comments | ~385 |
| Task Tags | ~2860 |
| Task SubTasks | ~2060 |
| Task Time Entries | ~500 |

## 🔍 التحقق من البيانات

بعد تشغيل Seeders، يمكنك التحقق من البيانات:

```bash
# عرض عدد السجلات في كل جدول
php artisan tinker
```

ثم في Tinker:
```php
User::count();           // يجب أن يكون 20
Project::count();        // يجب أن يكون 52
Task::count();           // يجب أن يكون ~572
App\Models\AI\AIDecision::count();  // يجب أن يكون ~250-300
```

## 🐛 استكشاف الأخطاء

### خطأ "Class not found"
```bash
composer dump-autoload
```

### خطأ في الذاكرة
```bash
php -d memory_limit=1024M artisan db:seed
```

### خطأ "SQLSTATE[23000]: Integrity constraint violation"
```bash
# احذف البيانات القديمة أولاً
php artisan migrate:fresh
php artisan db:seed
```

## 📝 معلومات إضافية

- **النسخة:** 2.5.0
- **تاريخ التحديث:** 9 يناير 2026
- **Laravel Version:** 11.x
- **PHP Version:** 8.2+

---
✅ **جاهز للاستخدام!** قم بتشغيل `php artisan migrate:fresh --seed` للبدء
